# Flask app to print the current IP and hostname to a web page on demand
Create a web app that prints the local IP to the browser (on HTTP - port 80)

                                                             
